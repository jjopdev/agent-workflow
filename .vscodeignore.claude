# Exclude everything, then whitelist only what Claude variant needs
**

# Extension core
!package.json
!src/**
!LICENSE

# Claude Code agents (canonical .md format, tools translated for VS Code)
!agents/**

# Shared skills
!skills/**

# Exclude development-only skill artifacts and domain-specific skills
**/evals/**
**/templates/**
**/references/**
skills/interface-design/**

# Claude rules (behavioral instructions)
!rules/**

# Consumer hooks template
!hooks/hooks.json

# Claude Code settings (permissions, sandbox)
!settings.json
