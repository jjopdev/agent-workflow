# Session Summaries

> Project-specific summaries are stored in `.github/tasks/summaries.md`. This file is a distribution template.
> When the plugin is installed in a new project, summaries accumulate in `.github/tasks/summaries.md`.
